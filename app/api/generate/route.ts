import {NextResponse} from "next/server";import {generateWithAI} from "../../../lib/ai";export async function POST(req:Request){try{const b=await req.json();if(!b.trend&&b.type!=="bio")return NextResponse.json({error:"Trend is required"},{status:400});const output=await generateWithAI({type:b.type||"caption",trend:b.trend||"Profile",niche:b.niche||"",language:b.language||"English",profile:b.profile||""});return NextResponse.json({output})}catch{return NextResponse.json({error:"Generation failed"},{status:500})}}