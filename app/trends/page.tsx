import {getTrends} from "../../lib/trends";import TrendCard from "../../components/TrendCard";export default async function Trends(){const trends=await getTrends();return <main className="container dashboard"><div className="topline"><div><h1>Trend Universe</h1><p>Explore content signals by platform, country and category.</p></div></div><div className="card"><div className="filters"><span className="pill active">🇧🇷 Brazil</span><span className="pill active">Instagram</span><span className="pill">YouTube</span><span className="pill">Pinterest</span><span className="pill">X</span></div><div className="trendlist">{trends.map(t=><TrendCard key={t.id} trend={t}/>)}</div></div></main>}