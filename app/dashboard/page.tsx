import {getTrends} from "../../lib/trends";import TrendCard from "../../components/TrendCard";import CreatePanel from "../../components/CreatePanel";export default async function Dashboard(){const trends=await getTrends();return <main className="container dashboard"><div className="topline"><div><div className="eyebrow">● LIVE-READY FEED</div><h1 style={{marginTop:12}}>What's trending</h1><p>Brazil 🇧🇷 · Instagram · trend signals and content opportunities</p></div><div className="filters"><span className="pill active">All</span><span className="pill">Instagram</span><span className="pill">Reels</span><span className="pill">YouTube</span><span className="pill">X</span></div></div><div className="grid"><div className="card span8"><div className="cardhead"><div><h2>🔥 Trend Radar</h2><p className="muted tiny">Starter feed — connect a live provider in lib/trends.ts.</p></div></div><div className="trendlist">{trends.map(t=><TrendCard key={t.id} trend={t}/>)}</div></div><div className="span4"><CreatePanel defaultTrend={trends[0]?.title}/></div></div></main>}